trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

steps:
- checkout: self

- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.9'

- script: |
    python -m pip install --upgrade pip
    pip install -r requirements.txt
    pip install locust  # Ensuring locust is present
  displayName: 'Install Dependencies'

- script: |
    echo "--- Current Directory Structure ---"
    ls -R
    
    echo "--- Starting Locust Load Test ---"
    # Running in headless mode for 2 minutes as an example
    # --run-time 2m: How long to run
    # --users 10: Number of users
    # --spawn-rate 2: How fast to start users
    locust -f locustfile.py --headless --users 10 --spawn-rate 2 --run-time 2m --host https://your-target-app.com
  displayName: 'Run Locust Load Test'
  env:
    LOCUST_ENV: 'prod'
    PYTHONPATH: $(System.DefaultWorkingDirectory) # This fixes the "No module named tests" error

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '$(System.DefaultWorkingDirectory)'
    artifact: 'LocustFiles'
    publishLocation: 'pipeline'
  condition: always()